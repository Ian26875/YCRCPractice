
@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>



<div class="well">
    @using (Html.BeginForm(""))
    {
        <div class="form-horizontal">
            <div class="form-group">
                <label for="ShipName" class="col-sm-2 control-label">收件人名稱</label>
                <div class="col-sm-10">
                    <input type="text" class="form-control" id="ShipName" placeholder="收件人名稱">
                </div>
            </div>
            <div class="form-group">
                <div class="col-sm-10"></div>
                <div class="col-sm-2">
                    <div class="dropdown">
                        <button class="btn btn-primary dropdown-toggle" type="button" data-toggle="dropdown">
                            ExportFile
                            <span class="caret"></span>
                        </button>
                        <ul class="dropdown-menu">
                            <li><a href="@Url.Action("ExportCsv")">Csv</a></li>
                        </ul>
                    </div>
                </div>
            </div>
        </div>
    }
</div>

@Html.Action("List")